name: Share Data

on:
  push

jobs:
  share:
    runs-on: ubuntu-latest
    steps:
        - id: srouce
          run: echo "result=Hello" >> "${GITHUB_OUTPUT}"
        - env:
            RESULT: ${{ steps.source.outputs.result }}
          run: echo "${RESULT}"
